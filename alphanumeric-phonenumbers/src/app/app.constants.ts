/**
 * This type of configuration could be generated dynamically
 * possibly based on environment variables.
 * This is simpler for a demo-app.
 */
export const appConfiguration = {
  pageSize: 10,
  baseUrl: 'http://localhost:8080/api'
};
